/**
 * Delay before work/project cards may begin their entrance, so they run after
 * the hero (headline stagger, bio, actions). Kept short so the work section feels snappy.
 */
export const HERO_INTRO_GATE_MS = 900;
